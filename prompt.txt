Fix a persistent bug: when the user opens field management from the inline recording form on Screen 1 and returns, field changes are not visible. The current fix reloads fields when isFormExpanded becomes true, but if the form is already open when the user returns from field management, isFormExpanded does not change and the reload does not fire. Fix this by also triggering reloadFieldConfigs when the Screen 1 component regains focus using useFocusEffect, in addition to the existing isFormExpanded trigger. Test specifically the case where the form is already expanded when the user navigates away to field management and returns. Run all tests and confirm they pass. 
