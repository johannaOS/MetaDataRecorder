Fix a missed migration: app/metadata.tsx still imports from expo-av which has been removed. Check what it uses from expo-av and replace with the equivalent from expo-audio. Most likely it is using Audio.Sound for playback preview or Audio.setAudioModeAsync. Replace all expo-av imports and usage in metadata.tsx with expo-audio equivalents. Run all tests and confirm they pass. 
