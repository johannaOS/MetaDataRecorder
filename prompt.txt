Fix several bugs after expo-audio migration. Bug 1: the timer in Screen 1 shows a long number string instead of MM:SS format. recorder.currentTime from useAudioRecorderState returns seconds as a float. The timer display code expects milliseconds. Fix the timer to correctly convert recorder.currentTime in seconds to a MM:SS display string. Bug 2: the waveform visualizer is not working. It was previously driven by metering data from expo-av getStatusAsync. With expo-audio use recorder.getStatus().metering from useAudioRecorderState to drive the waveform animation. Bug 3: the saved duration shown in the library list is wrong. After stopping, get the actual duration from the saved file metadata rather than from the JS timer. Use expo-file-system or the recorder state to get the correct duration in milliseconds after stop. Bug 4: the permissions popup still appears after stopping. Audit the full stop flow in index.tsx and metadata.tsx for any remaining calls that trigger Android permission dialogs. Bug 5: when recording starts, any inline playback in Library should stop. Add an AppState or event-based mechanism so that starting a recording stops any active audio player. Bug 6: when navigating away from Library, stop any inline playback. In library.tsx use useFocusEffect to stop and release the audio player when the screen loses focus. Run all tests and confirm they pass. 
