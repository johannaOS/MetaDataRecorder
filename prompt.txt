Fix one bug: after saving on Screen 2 metadata form, the app navigates to Screen 1 idle instead of the Library. Fix router.replace in handleSave in metadata.tsx to navigate to /library not /. Add a test verifying this navigation. Run all tests and confirm they pass. 
