Fix the following bugs and update unit tests accordingly. Bug 1: on Screen 2 metadata form, changes made in the field management screen such as deactivating or adding fields must reflect immediately when returning to the form without requiring a save. Fix the field list to reload every time Screen 2 gains focus. Add a unit test that verifies visible fields update immediately after a hide toggle. Bug 2: the field management settings icon must appear on the edit form in Screen 4 and when editing from the Library long press, not only when saving a new recording. Bug 3: replace the settings cog wheel icon with a pencil edit icon on Screen 2 metadata form. Bug 4: fix the autofill logic so that extractOfAfter stops at the word "fr†n" and does not include it or anything after it. For example "av Axel Samuelsson fr†n Dalarna" should extract only "av Axel Samuelsson" for the of/after field and "Dalarna" for the from field. Add unit tests covering this exact case and similar compound cases. Run all tests and confirm they pass before finishing. 
