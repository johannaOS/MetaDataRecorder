Fix audio file storage to save into a real subfolder. Instead of using MediaLibrary.createAssetAsync which cannot create real subfolders on Android, use expo-file-system to write directly to the device's Music/VoiceRecorder folder using StorageAccessFramework or the standard external storage path. The folder must appear as Music/VoiceRecorder in the device file manager. Keep the existing filename logic including conflict resolution and Swedish character support. If direct filesystem access is not possible due to Android scoped storage restrictions on this version of expo-file-system, investigate and implement the correct alternative approach for Android 10 and above. Update the file path stored in the database. Run all tests and confirm they pass. 
