Fix four bugs in the app. Bug 1 - Screen lock stops recording: the recording stops when the screen locks. This worked in a previous build. The fix is to ensure setAudioModeAsync with allowsBackgroundRecording true is called correctly before recording starts on Android, and verify the expo-audio config plugin has enableBackgroundRecording true in app.json. Also check if there is an AppState handler that is incorrectly pausing or stopping recording when the app state changes to background or inactive. Bug 2 - Files not saved to Music/VoiceRecorder folder: recordings appear in the library and play back correctly, so the temp file in cache exists, but saveRecording.ts is failing to copy it to the Music folder. Read lib/saveRecording.ts carefully and fix the file copy logic. The recorder URI from expo-audio is a file:// URI in the app cache. The save function must copy it to the correct destination. Add console.log statements at each step so failures are visible in logs. Bug 3 - Timer does not catch up when returning to app after background recording: elapsed time shown on Screen 1 does not update to reflect actual recording duration after returning from background. Since recorderState.durationMillis is the source of truth, the timer display should always show the current recorderState.durationMillis divided by 1000, which will automatically reflect the correct elapsed time when the component re-renders on foreground. Verify this is wired correctly. Bug 4 - Library shows wrong duration: the duration saved to the database after stopping is incorrect. After recorder.stop() resolves, use recorderState.durationMillis as the duration, or read the actual file duration from the saved file using expo-av or expo-file-system. Run all tests and confirm they pass. 
