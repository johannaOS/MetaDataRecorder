Fix the following bugs and fixes. Bug 8: the app navigation stack grows with every action so pressing back many times is required to exit. Implement proper navigation so Screen 1 and Screen 3 are root screens, Screen 2 and Screen 4 are pushed on top and popped correctly. Going back from Screen 4 should go to Screen 3, going back from Screen 3 should exit the app, going back from Screen 1 should exit the app. Bug 9: disabling a field in field management while recording does not hide it in the inline form. This was previously fixed but is still broken in the inline recording form specifically. Investigate and fix. Fix 10: limit autofill of the of/after field to a maximum of 2 words after the keyword, and limit autofill of the from field to a maximum of 1 word after fr†n. Fix 11: add Polon„s to the list of recognised song type keywords in autoFill.ts and to the default filter keywords. Fix 12: tapping a list item in the Library should go to Screen 4 but not auto-play. Remove the auto-play on arrival behaviour. Fix 13: add a small x button on the right side of the search bar in the Library that clears the search text immediately. Show it only when the search field has text. Fix 14: filter keywords in the Library should match against the songType field but also against the recording title. A recording titled polon„s should show when the polon„s filter is active. Fix 15: when deleting a custom field that has data stored in existing recordings, show a confirmation prompt in Swedish warning the user how many recordings have data in that field and that deleting will permanently remove that data. Run all tests and confirm they pass. 
