Recording stopped working after the last build. Logs show the app opens correctly but the record button does nothing. The JS thread appears stuck during startup. The most likely cause is the new PermissionsAndroid.requestMultiple call added in _layout.tsx. Fix by wrapping the entire permission request block in a try-catch so any error is caught and logged without crashing. Also add a console.log before and after each permission request so we can see exactly where it gets stuck. Additionally check if PermissionsAndroid from react-native conflicts with the existing expo-media-library permission request - if both are requesting the same permissions simultaneously it could cause a deadlock. Run all tests and confirm they pass. 
