Fix navigation stack behaviour throughout the app. Screen 1 (recorder) and Screen 3 (library) must be root screens - pressing back from either should exit the app, never navigate to another screen. Screen 2 (metadata form) must always pop back to Screen 1 after saving or discarding. Screen 4 (playback/detail) must always pop back to Screen 3 when pressing back. The field management screen must pop back to whichever screen opened it. Audit every router.push, router.replace, and router.back call in the entire codebase and correct them. Use router.replace for navigating between root screens and for post-save navigation that should not be back-trackable. Use router.push only for screens that should be dismissible with back. After fixing, the only valid back navigation paths are: Screen 2 back goes to Screen 1, Screen 4 back goes to Screen 3, field management back goes to the opener, everything else exits the app. Run all tests and confirm they pass. 
