Fix three bugs. Bug 1: saving a recording with the same title as an existing file throws an error instead of appending a suffix. The generateSafeFilename conflict resolution logic exists but is not being called correctly when the file already exists in the VoiceRecorder folder. Fix it so duplicate titles automatically get (1), (2) etc appended and never show an error. Also make error messages more specific - when saving fails, show the actual reason not just a generic Swedish error. Bug 2: the modify audio permissions popup still appears every time recording stops on Android. The previous fix made setAudioModeAsync iOS-only but the popup is still appearing - investigate what is triggering the permission check on Android specifically in the stop flow and eliminate it. Bug 3: tapping a playing list item in Library to open Screen 4 does not continue playback. The audio handoff from Library inline player to Screen 4 is not working. Pass the current playback position and playing state to Screen 4 via navigation params and have Screen 4 resume from that position immediately on load. Stop the Library inline player before navigating. Run all tests and confirm they pass. 
