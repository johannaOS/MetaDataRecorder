I want you to always create new tests when applicable, when fixing a bug or building a new feature. Also notify me when you've done so.

Fix 1: export single file
from screen 4 -> export -> share screen appears -> press on add to drive
expected: go back to screen 4 and a dissapearing pop up vissible for 2-3 s saying something like "your file has been exported"
actual: goes back to screen 4, no pop up.

Fix 2: export files
from library -> long press and select several recordings -> tap on share button
expected: prompt if want to export only audio files or audiofiles + meta data. If only audiofiles selected -> share audiofiles, not in zip format, if selected "audio + metadata" share as zip file (as it is now)
actual: share zip file directly, even if only 1 item selected.

After this we are ready for to build to upload on playstore.